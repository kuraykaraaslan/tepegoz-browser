# Hoisted linker eases Electron + native module (better-sqlite3/sqlite-vec) + electron-builder install-app-deps
node-linker=hoisted
strict-peer-dependencies=false
# Native modules are rebuilt against Electron's ABI in CI / via electron-builder, not at install time here
