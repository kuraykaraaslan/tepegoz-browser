# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
out/
build/
.turbo/
*.tsbuildinfo

# Native modules (rebuilt per-OS in CI)
*.node

# Test / coverage
coverage/
test-results/
playwright-report/
# Eval output is REGENERABLE, never a source of truth — a committed report goes stale the moment the
# scoring logic changes, and then it lies. The durable record is phases/ai-agent-super/eval-results.md.
# Regenerate: phases/ai-agent-super/eval-loop-runbook.md
agent-eval-report.json
agent-eval-runs/
*.log

# Secrets & local config (NEVER commit) — BYO keys live in OS keychain, not files
.env
.env.local
.env.*.local

# Packaged artifacts
/release/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
