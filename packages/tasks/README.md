# @tepegoz/tasks

Electron-free task productization domain: saved agent tasks, trigger definitions, redacted run/artifact
records, reducer/selectors, main-only zod schemas, and `task_*` Capability Plane tool registration.
