[package]
name = "tepegoz-native"
version = "0.0.0"
edition = "2021"
publish = false

# PLACEHOLDER crate (Phase 1b). Not part of the JS/turbo build — there is intentionally no package.json
# here, so pnpm does not treat it as a workspace package and CI does not require a Rust toolchain yet.

[lib]
crate-type = ["cdylib"]

[dependencies]
# napi = { version = "2", default-features = false, features = ["napi8"] }
# napi-derive = "2"

[build-dependencies]
# napi-build = "2"
