/** Constant model-catalog/downloader messages (internal-ai-rules: no inline throw strings). */
export const CatalogMessages = {
  DownloadCanceled: 'Model download canceled',
  ChecksumMismatch: 'Downloaded model failed its integrity check (sha256 mismatch)',
  ModelNotInCatalog: 'Unknown model id',
} as const;
