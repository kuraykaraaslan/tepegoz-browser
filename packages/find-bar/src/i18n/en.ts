export const en = {
  /** Input placeholder — Chrome's wording for the same bar. */
  placeholder: 'Find in page',
  /** Shown in the counter slot when the query matches nothing. */
  noResults: 'No results',
  /** Screen-reader label for the "n of m" counter (the numbers themselves are not translated). */
  matchCount: 'Match count',
  previous: 'Previous match',
  next: 'Next match',
  close: 'Close find bar',
  matchCase: 'Match case',
};

export type FindBarStrings = typeof en;
