{
  "name": "@tepegoz/i18n",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./react": "./src/react.tsx",
    "./testing": "./src/testing.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "peerDependencies": {
    "react": "^18 || ^19"
  },
  "peerDependenciesMeta": {
    "react": {
      "optional": true
    }
  },
  "devDependencies": {
    "@tepegoz/security-policy": "workspace:*",
    "@types/react": "^18.3.18",
    "typescript": "^5.7.2",
    "vitest": "^3.2.6"
  },
  "//devDependencies": "`@tepegoz/security-policy` is a DEV dependency only: the permission-explanation parity test imports POLICY_REASONS so the catalogue and its text cannot drift. Nothing at runtime in this package depends on it, which keeps the dependency arrow from inverting."
}
