/**
 * `@tepegoz/window-controls` — native caption buttons (minimize / maximize·restore / close) for a
 * frameless window. Pure view; maximized state + actions are injected. Extracted from `apps/desktop`
 * per docs/package-map.md.
 */
export {
  WindowControls,
  type WindowControlsProps,
  type WindowControlsLabels,
} from './window-controls';
export * from './caption-layout';
