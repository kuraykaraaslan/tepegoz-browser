# @tepegoz/uploads-ui

Presentational `tepegoz://uploads` activity surface. The host injects list/command/subscribe
callbacks; this package owns only UI state and en/tr strings. Upload records are redacted: local
paths and content never enter this renderer package.
