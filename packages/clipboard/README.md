# @tepegoz/clipboard

Headless clipboard policy/types package. It centralizes operation metadata and approval defaults while
keeping clipboard contents out of persistent state, logs, and Event Journal payloads.
