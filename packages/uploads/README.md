# @tepegoz/uploads

Electron-free upload broker domain model for browser file uploads. It owns redacted upload records,
status/risk helpers, zod schemas, and `upload_*` Capability Plane registration. Desktop owns file paths,
CDP file-input binding, native dialogs, and Event Journal audit.
