# @tepegoz/downloads

Headless browser-download domain package. It owns preload-safe public types, reducer/selectors, and
trust/risk helpers. Electron, filesystem paths, quarantine moves, and IPC live in the desktop host.
