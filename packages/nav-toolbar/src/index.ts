/**
 * `@tepegoz/nav-toolbar` — the browser navigation bar (back/forward/reload + omnibox + actions slot +
 * menu). Actions injected via callbacks. Extracted from `apps/desktop` per docs/package-map.md.
 */
export {
  NavToolbar,
  NAV_BTN,
  type NavToolbarProps,
  type NavToolbarLabels,
} from './nav-toolbar';
